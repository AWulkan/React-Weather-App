const React = require("react");

const WeatherMessage = React.createClass({
    render: function () {
        return (
            <h3>It is 0 degrees in Sweden</h3>
        );
    }
});

module.exports = WeatherMessage;
